spring:
  application:
    name: spring-batch
  config:
    import: file:optional:.env[.properties]

  datasource:
    hikari:
      maximum-pool-size: 10
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:batch
    username: sa
    password: